---
title: loom config init
description: Initialize loom config
---

# loom config init

Initialize loom config

## Usage

```bash
loom config init
```

## Examples

```bash
# Initialize config in current directory
loom config init

# Initialize with a specific path
loom config init --config /path/to/loom.yaml
```
## See Also

- [`loom config`](/cli-reference/loom-config) - Manage configuration
- [`loom config show`](/cli-reference/loom-config-show) - Show current configuration
